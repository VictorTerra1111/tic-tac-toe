#ifndef BOARD_HPP
#define BOARD_HPP

#include <vector>

using namespace std;

bool vector_find(const std::vector<int>& vec, int value);

bool draw_board(const vector<int>& x_pos, const vector<int>& o_pos);

#endif